fn sayhello(message):
    print("Hello " + message);
end

fn main:
    world = "World!";
    hello(world) # => "Hello World!";

    val world = "World again!";
    hello(world) # => "Hello World again!";
end